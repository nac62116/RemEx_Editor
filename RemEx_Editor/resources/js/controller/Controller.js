/* eslint-env browser */

// App controller controls the program flow. It has instances of all views and our model (Storage).
// It is the communication layer between our views and our data model.

class Controller {

    init() {}
    
}

export default new Controller();